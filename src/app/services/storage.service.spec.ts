import { TestBed } from '@angular/core/testing';

import { StorageService } from './storage.service';

describe('StorageService', () => {
  it('deberia crear el servicio storageservice', () => {
    expect(true).toBe(true);
  });
});
